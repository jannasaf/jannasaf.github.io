# The generated pages (index.html, about.html, work/, assets/css/) are committed on
# purpose: GitHub Pages serves this repo's root, so they are the deployed site.
# Regenerate with `python3 build.py`.

# throwaway builds from `python3 build.py <dir>`
/preview/

# archived earlier attempt: keep the source, not its dependencies or build
archive/codex-astro/node_modules/
archive/codex-astro/dist/
archive/codex-astro/.astro/

# agent scratch
.agents/
.codex/
.preview.log

# python
__pycache__/
*.pyc

# os
.DS_Store
*.log
.env
.env.*
!.env.example
